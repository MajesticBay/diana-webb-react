import React from 'react';

// const Button = ({ openPopup, setOpenPopup }) => (
//   <button className="btn pointer" onClick={() => setOpenPopup(!openPopup)}>
//     Order the Book
//   </button>
// );

const Button = () => <button className="btn pointer">Order the Book</button>;

export default Button;
